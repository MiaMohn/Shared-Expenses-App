package com.autentia.sharedexpenses.shared_expenses_app;

/*import static org.mockito.Mockito.times;
import static org.mockito.Mockito.verify;

public class CreateUserControllerTest {

    private final UserService service = Mockito.mock(UserService.class);
    private final RestUserController sut = new RestUserController(service);

    @Test
    public void shouldCreateUser() {

        final UserRequest request = new UserRequest(
                123,
                "Maria"
        );

        final User expectedUser = new User(
                123,
                "Maria"
        );

        sut.createUser(request);

        verify(service, times(1)).createUser(expectedUser);

    }
}*/

